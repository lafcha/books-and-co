{% extends 'base.html.twig' %}

{% block title %}Livre{% endblock %}

{% block main %}
<div>
    <p>{{ book.isbn }}</p>
    <p>{{ book.title }}</p>
    <p>{{ book.author }}</p>
    <p>{{ book.cover }}</p>
    <p>{{ book.editor }}</p>
    <p>{{ book.year }}</p>
    <p>{{ book.description }}</p>
</div>
<div>
<a href="{{ path('library_book_edit', {'slug': book.slug}) }}">edit</a>
</div>
{% endblock %}
