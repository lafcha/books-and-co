{% extends 'base.html.twig' %}

{% block title %}Hello LibraryController!{% endblock %}

{% block main %}
{% if usersBooks == null %}
    <p>Vous n'avez aucun livre dans votre bibliothèque</p>
{% else %}
    <div>
        <ul>
            {# if the user connected is equal to the library's user AND if usersBooks is not null #}

                {% for usersBook in usersBooks %}
                    {# usersBook contains all books of a user, use usersBook.book to get all data from a book #}
                    <li><a href="{{ path('library_book_read', {'userSlug': usersBook.user.slug, 'slug': usersBook.book.slug }) }}">{{ usersBook.book.title }}</a></li>
                {% endfor %}
        </ul>
    </div>
    <ul class="pagination">
        <li class="{{ (currentPage == 1) ? 'disabled' : '' }}">
            <a href="?page=1">
                <i class="fas fa-step-backward">début</i>
            </a>
        </li>    
        <li class="{{ (currentPage == 1) ? 'disabled' : '' }}">
            <a href="{{ (currentPage > 1) ? '?page=' ~ (currentPage - 1) : '' }}">
                <i class="fas fa-angle-left">précédent</i>
            </a>
        </li>
        {% set pages = (usersBookTotal / usersBookLimit)|round(0, 'ceil') %}
        {% for item in 1..pages %}
            <li class="{{ (currentPage == item) ? 'active' : '' }}">
                <a href="?page={{ item }}">{{ item }}</a>
            </li>
        {% endfor %}
        <li class="{{ (currentPage == pages) ? 'disabled' : '' }}">
            <a href="{{ (currentPage < pages) ? '?page=' ~ (currentPage + 1) : '' }}">
                <i class="fas fa-angle-right">suivant</i>
            </a>
        </li>
        <li class="{{ (currentPage == pages) ? 'disabled' : '' }}">
            <a href="?page={{ pages }}">
                <i class="fas fa-step-forward">fin</i>
            </a>
        </li>
    </ul>
    {% if app.user.slug == app.request.attributes.all['userSlug'] %}
        <div>
            <a href="{{ path('library_book_add', {'userSlug': app.user.slug}) }}">ajouter un livre</a>
        </div>
    {% endif %}
{% endif %}
{% endblock %}
